from facebookApp import app

if __name__ == "__main__":
    app.run(debug=True)
# this means that the App will be launched in debug mode
# in case of errors we'll get a page with errors instead of a blank one
